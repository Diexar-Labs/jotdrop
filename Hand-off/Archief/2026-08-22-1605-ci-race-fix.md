ARCHIEF - NIET ACTUEEL

# JotDrop Handoff

## Source Of Truth

Verified on 2026-08-22 against the live GitHub remote:

- Repository: `https://github.com/Diexar-Labs/jotdrop.git`
- Canonical ref: `origin/main`
- Canonical commit: `2530426dae81a5330fc4009db2db0fa61cae9107`
- Obsidian plugin version: `0.20.1`
- Android app version: `0.28.1`, versionCode `55`
- Commit pushed to GitHub: `2530426` (`main`)

Refresh the remote before relying on these values. The clean port checkout is
the only permitted implementation base for the latest-source work.

## Unsafe Checkouts

The primary checkout at `F:\New Dee\My Business\App IDE\Obsidian Keep Plugin`
is behind `origin/main` and contains unrelated uncommitted work. Do not reset,
clean, build releases, or deploy from it.

The temporary checkout at
`C:\Users\Proteus\AppData\Local\Temp\kilo\jotdrop-ime-release` has stale
uncommitted source deletions. Do not use that working tree as a build or
recovery source.

## Completed Changes

- Mobile loading no longer evaluates the Node clipserver on Obsidian Mobile.
- Desktop and Android tag input accepts comma/space separators and preserves
  focus after committing a tag.
- Android editor focus and keyboard layout are corrected.
- Plugin horizontal overflow is bounded on narrow screens.
- Android creates the configured notes subfolder when the selected vault is
  writable, and empty-state text remains readable on dark themes.
- Release provenance, APK verification, install, and explicit deploy gates are
  committed in the repository.

## Verification

- `npm run verify:base` passed.
- `npm run build` passed without vault deployment.
- `npm run verify:deploy` passed after commit.
- `npm run android:test-release` passed with R8, package
  `com.diexar.keepcapture`, version `0.28.1`, versionCode `55`, and valid signer.
- Plugin was tested on PC and Obsidian Android by the user.
- Android physical-device test passed, including tag entry with comma
  separators and renewed SAF access to the vault.

The accepted Android artifact path is:

`android/app/build/outputs/apk/release/app-release.apk`
